# Copy this file to .env and populate the values before running make.
# Do not commit your real AWS account IDs or repo names.

# Required for AWS SSO authentication
SSO_ACCOUNT_ID=

# Required for GitHub OIDC setup (if using github-oidc.tf)
TF_VAR_github_repo=
# Optional overrides:
# SSO_START_URL=
# SSO_REGION=
# SSO_ROLE_NAME=
# AWS_REGION=
